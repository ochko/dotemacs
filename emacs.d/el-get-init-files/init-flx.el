(require 'flx-ido)
(ido-mode 1)
(ido-everywhere 1)
(flx-ido-mode 1)
;; can disable ido faces to see flx highlights.
(setq ido-enable-flex-matching t)
(setq ido-use-faces t)
;; can disable flx faces
(setq flx-ido-use-faces t)
